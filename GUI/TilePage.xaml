<Page x:Class="GUI.TilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GUI"
      mc:Ignorable="d" 
      Title="TilePage" MinWidth="600" MinHeight="300">


    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <StackPanel x:Name="stack1" Orientation="Horizontal" ScrollViewer.CanContentScroll="True">
                <Frame x:Name="PageView" NavigationUIVisibility="Hidden"/>
            </StackPanel>
            <StackPanel x:Name="stack2" Orientation="Horizontal" ScrollViewer.CanContentScroll="True" Grid.Row="1">
                <Frame x:Name="PageView2" NavigationUIVisibility="Hidden"/>
            </StackPanel>
            
        </Grid>
    </ScrollViewer>


</Page>
