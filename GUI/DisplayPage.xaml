<Page x:Class="GUI.DisplayPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GUI"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="300"
      Title="DisplayPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="300"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Image x:Name="image" Height="280" Width="280"/>

        <StackPanel Grid.Row="1">
            <TextBlock x:Name="title" Grid.Row="1" TextWrapping="Wrap" Text="Titel" VerticalAlignment="Top" Foreground="#FFEAEAEA" FontSize="20" TextAlignment="Center"/>
        </StackPanel>

    </Grid>
</Page>
